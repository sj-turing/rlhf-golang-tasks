// filename: main.go
package main

/*

import (
	"fmt"

	"github.com/goware/urlx"
)

func parseURL(inputURL string) error {
	// Parse the URL using urlx
	u, err := urlx.Parse(inputURL)
	if err != nil {
		return fmt.Errorf("error parsing URL: %w", err)
	}

	// Extract URL components
	scheme := u.Scheme
	host := u.Host
	path := u.Path
	queryParameters := u.Query()

	// Display URL components
	fmt.Printf("Scheme: %s\n", scheme)
	fmt.Printf("Host: %s\n", host)
	fmt.Printf("Path: %s\n", path)
	fmt.Printf("Query Parameters: %v\n", queryParameters)

	return nil
}

func main() {
	inputURL := "https://api.example.com/data?key=value&anotherKey=anotherValue"

	if err := parseURL(inputURL); err != nil {
		fmt.Println("Error parsing URL:", err)
	}
}
*/
