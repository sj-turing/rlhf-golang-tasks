package main

/*

import (
	"fmt"
	"time"
)

func processChunk(chunk []int) {
	for _, value := range chunk {
		fmt.Println("Processing chunk value:", value)
		time.Sleep(100 * time.Millisecond)
	}
}

func main() {
	data := make([]int, 1000)
	for i := 0; i < len(data); i++ {
		data[i] = i
	}

	chunkSize := 100
	for i := 0; i < len(data); i += chunkSize {
		chunk := data[i : i+chunkSize]
		processChunk(chunk)
	}
}
*/
